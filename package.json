{
  "name": "proto-agent",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=./packages/server --filter=./packages/app",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.18.2",
  "devDependencies": {
    "turbo": "^2.8.17"
  }
}
